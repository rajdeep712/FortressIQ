fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

httpx==0.27.0

boto3==1.38.36
botocore==1.38.36

sqlalchemy==2.0.41
pydantic==2.11.4
pydantic-settings==2.9.1

python-magic==0.4.27
python-magic-bin==0.4.14; platform_system == "Windows"

python-dotenv==1.1.0

# Auth
pyjwt[crypto]
bcrypt
email-validator

pytest==8.3.2
openpyxl==3.1.5
beautifulsoup4
pymupdf

google-genai
qdrant-client
arq
redis

# Local embeddings (torch CPU):
# pip install --index-url https://download.pytorch.org/whl/cpu torch
sentence-transformers

# Retrieval: BM25 sparse embeddings + cross-encoder reranking
fastembed
flashrank
onnxruntime

# PostgreSQL (BYO connection string)
psycopg[binary]

# LLM generation (Groq) + LangGraph (per-chat persistent memory + graph)
langgraph
langchain-core
langchain-groq
langgraph-checkpoint-postgres

# Observability: Arize Phoenix (OpenTelemetry / OpenInference)
arize-phoenix-otel>=0.16.0
openinference-instrumentation-langchain
opentelemetry-instrumentation-qdrant
opentelemetry-instrumentation-httpx

# Job-queue monitoring: ARQ web dashboard (mounted at /worq in FastAPI)
worq-dashboard[ui]
